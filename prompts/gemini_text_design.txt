Generate one high-resolution PNG.
Background must be a flat, solid pure black (#000000) matte.
Do not output transparency preview/checkerboard patterns. Do not simulate alpha.
No gradients, noise, texture, or vignette in the background; keep the background uniformly black.

Use the reference image as the style source for:
- Collegiate block letter construction
- Bevel / metallic depth
- Gloss highlight behavior
- White outer stroke and soft white outer glow
- Kerning, weight, and spacing proportions

CRITICAL GEOMETRY RULE (MOST IMPORTANT):
- The top silhouette/cap line of the whole word must stay visually straight and level from first letter to last letter.
- Only the bottom contour / baseline should form a smooth upward concave curve (U-shaped rise toward center).
- Do NOT apply an envelope warp that bends both top and bottom edges.
- Do NOT produce a rainbow-arch where both upper and lower contours are curved.
- Keep letter height consistency and upright block structure while following the curved baseline at the bottom.

Color theme requirement:
[COLOR_SCHEME]

Technical quality requirements:
- Very sharp, print-friendly rendering (clean edges, no blur, no compression artifacts, no pixelation)
- Preserve interior counters/holes (A, O, P, R, B, etc.) with clean letterforms
- Keep the white stroke and glow crisp over black background
- Output should be optimized for downstream background-removal processing

Replace the text with exactly:
[TARGET_TEXT]
